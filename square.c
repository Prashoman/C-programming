#include <stdio.h>
int main()
{

    float length, square_are;

    printf("Enter the value of length : ");
    scanf("%f",&length);


    square_are = (length*length);

    printf("Total Rectangle Area is : %.2f",square_are);


    return 0;


}

